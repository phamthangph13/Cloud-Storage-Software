import 'package:flutter/material.dart';

class NhanAIPage extends StatelessWidget {
  const NhanAIPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AI Labeling'),
      ),
      body: const Center(
        child: Text('AI Labeling Page'),
      ),
    );
  }
}